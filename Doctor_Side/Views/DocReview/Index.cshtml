@model IEnumerable<Doctor_Side.Models.DocReview>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="page-content">
    <div class="container-fluid">
        <!-- start page title -->
        <div class="row">
            <div class="col-12">
                <div class="page-title-box d-flex align-items-center justify-content-between">
                    <h4 class="mb-0 font-size-18">Review List</h4>
                    <div class="page-title-right">
                        <ol class="breadcrumb m-0">
                            <li class="breadcrumb-item"><a href="../Home1/Index">Dashboard</a></li>
                            <li class="breadcrumb-item active">Review List </li>
                        </ol>
                    </div>
                </div>
            </div>
        </div>
        <!-- end page title -->
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <div class="row mb-2">
                            <div class="col-sm-4">
                            </div>
                            <div class="col-sm-8">
                                <div class="text-sm-right">
                                    <!--<button type="button" class="btn btn-success btn-rounded waves-effect waves-light mb-2 mr-2"><i class="mdi mdi-plus mr-1"></i><a class="mr-1" href="vendor_product_add.php"> Add New Product</a></button>-->
                                    
                                </div>
                            </div><!-- end col-->
                        </div>
                        <div class="table-responsive">
                            <table class="table table-centered table-nowrap">
                                <thead class="thead-light">
                                    <tr>
                                        <th>Patient Profile</th>
                                        <th>Patient Name</th>
                                        <th>Review</th>
                                       
                                        
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td class="align-middle">
                                                <img src="~/_content/Medical/Patient_Image/@item.Patient_Profile" height="50" width="50" class="rounded-circle" />
                                            </td>
                                            <td class="align-middle">@Html.DisplayFor(modelItem => item.Patient_Name)</td>
                                            <td class="align-middle">@Html.DisplayFor(modelItem => item.Review_Msg)</td>
                                            
                                            
                                        </tr>
                                    }
                                </tbody>

                            </table>
                            @*<div class="alert alert-danger text-center mb-4" role="alert">
                                    @ViewBag.Message
                                </div>*@
                        </div>


                        <ul class="pagination pagination-rounded justify-content-end mb-2">
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        <!-- end row -->
    </div> <!-- container-fluid -->
</div>
    @*<div class="main-content">
            <div class="page-content">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-12">
                            <div class="page-title-box d-flex align-items-center justify-content-between">
                                <h4 class="mb-0 font-size-18">Reviews</h4>
                                <div class="page-title-right">
                                    <ol class="breadcrumb m-0">
                                        <li class="breadcrumb-item"><a href="../Home1/Index">Dashboard</a></li>
                                        <li class="breadcrumb-item active">Reviews</li>
                                    </ol>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="card">
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-xl-12">
                                        <table class="table mb-0 table-bordered">
                                            <tr>
                                                <th>Patient Name</th>
                                                <th>Review</th>
                                            </tr>
                                            @foreach (var item in Model)
                                            {
                                                if (item != null)
                                                {
                                                    <tr>
                                                        <td>@Html.DisplayFor(modelItem => item.Patient_Name)</td>
                                                        <td>@Html.DisplayFor(modelItem => item.Review_Msg)</td>
                                                        <td><a href="vendor_product_details.php?prodid=<?php echo $s['product_id']; ?>"><img class="avatar-md img-thumbnail" src="<?php echo $s[" image"]; ?>"></a></td>
                                                    </tr>
                                                }
                                                else
                                                {
                                                    <tr><td colspan=3>There are No reviews for You</td></tr>
                                                }
                                            }

                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>*@
